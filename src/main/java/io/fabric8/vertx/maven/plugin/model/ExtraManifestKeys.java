package io.fabric8.vertx.maven.plugin.model;

/**
 * @author kameshs
 */
public enum ExtraManifestKeys {

    timestamp, userName, projectName, projectGroup, projectVersion, scmUrl, scmTag,
    scmRevision, commitId, projectUrl, projectDependencies,scmType
}
